function onCartComplete(){
	/*
	$(function(){
		ClickpdxCart.onComplete(function(responseData,request,event,data){
			var pricebookEntryId, error, message;
		

			error = responseData.error || false;
			message = responseData.message || '';
		
			console.log('responseData is:');
			console.log(responseData);
		
			if(error) {
				jQuery('.product-option-wrapper .loading').removeClass('loading');             	   
				alert('There was an error adding this item to your Cart: '+message);
				// currentTarget.addEventListener('click',window.addToCartButtonClick,false);
				return false;
			}
		
		pricebookEntryId = responseData.item.priceBookEntryId;
			$('.add-to-cart-donation-button').addClass('cart-item-added');
			$('.add-to-cart-donation-button').removeClass('loading');
			$('.add-to-cart-donation-button').text('Item Added');
		});
		
	});
		*/
}